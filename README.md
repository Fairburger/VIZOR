# Temporary RC4 Vite build farm

This repository is being used temporarily to build the frontend bundle for Zvonilka NEXT RC4. No backend code, database dumps, credentials, or staging configuration are included.
